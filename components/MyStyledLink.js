import styled, { css } from "styled-components";
import { StyledLink } from "./StyledLink";

export const MyStyledLink = styled(StyledLink)`
  // box-shadow: 3px 3px 8px rgba(0, 0, 0, 0.12);
  padding: unset;
  position: fixed;
  top: 24px;
  z-index: 99;
`;
